#pragma once

enum layers {
    // Base layers (keep at top or MO() won't work)
    LAYER_BASE = 0,
    LAYER_LINUX,
    LAYER_GAME,

    LAYER_MOUSE,
    LAYER_NAV,
    LAYER_NUM,
    LAYER_SYM,

    LAYER_CALL,
    LAYER_MPV,
    LAYER_NUMFN,
    LAYER_NUMGM,
    LAYER_NUMGI,
};
